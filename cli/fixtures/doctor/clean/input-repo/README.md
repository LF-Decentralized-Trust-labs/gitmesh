# Sample project

A repository that configures no coding agent at all: no AGENTS.md, no
CLAUDE.md, no `.cursor/`, no MCP config. `gitmesh doctor` must report an
empty inventory, no drift, no findings and a perfect score - and must keep
emitting every key of the schema while doing so.
