import type { RiskRule } from "./risk.js";

/**
 * The GM rule table. T1.11–T1.15 land GM001–GM011 here, in id order;
 * each rule ships with triggering and non-triggering fixtures.
 */
export const riskRules: readonly RiskRule[] = Object.freeze([] as RiskRule[]);
