---
"@gitmesh/workspace-core": minor
---

Complete the GM rule table (pivot §8.1 item 3, T1.15). `GM010` (warning) reports the #6235 finding on the root instruction pair: AGENTS.md with no CLAUDE.md shim, CLAUDE.md with no AGENTS.md for the other agents, or both present with neither an `@AGENTS.md` / `@./AGENTS.md` import token in CLAUDE.md nor a symlink either way; the import scan is deliberately lenient (a fenced example counts as bridged - erring silent beats nagging a repo that already solved #6235), unreadable content is treated as bridged, and nested pairs stay in the drift differ's lane. `GM011` (warning) reports semantic contradictions in a single tool's config - the claude-config-doctor pattern, on claude-code, the one §4.3 surface carrying permissions, hooks, imports, commands and subagents together: a dead allow (same entry in `permissions.allow` and `deny`/`ask`, exact match), a destructive allow (§10.5 `gate-destructive` shapes: `rm -rf`, `git push --force`, `curl … |`) pre-approving what a PreToolUse hook in the same file gates (absent/`*` matchers cover every tool; non-destructive allows never fire so logging hooks stay quiet), a dangling `@.claude/...` import (only the fully-inventoried namespace, so absence is decidable), and a `subagent_type` reference naming neither an inventoried `.claude/agents/` subagent (frontmatter `name:`, else basename) nor a long-stable built-in - checked only when the repo inventories at least one subagent, since a repo with none plausibly relies on agents the doctor cannot see. Other adapters' reference syntaxes join as the format-canary lane verifies them. Four new golden cases run the whole table.

A review pass over the full GM001–GM011 set rides along: GM001 now pairs a credential-named CLI flag with a value pretty-printed onto the next `args` line; GM002 counts only committed project-scope config as protection (a deny in one developer's user-scope settings protected no other clone yet silenced the finding); GM008 counts only *distinctive* artifacts as presence (a root AGENTS.md claimed by six adapters is evidence for none of them, killing the orphan fan-out); GM010 aggregates bridge evidence across duplicate inventory entries so the verdict is inventory-order independent; the §10.4 marker/fence grammar moves to one shared `normalizer/grammar.ts` consumed by the normalizer, GM006 and GM011 (ADR-003 is one contract, not three copies); and `makeGm007`/`GM007_DEFAULT_THRESHOLD` are exported so the T1.17 CLI can actually pass a repo-chosen threshold. The fixture harness rejects unknown `inventory.json` keys so a typo'd negative case cannot pass vacuously.
