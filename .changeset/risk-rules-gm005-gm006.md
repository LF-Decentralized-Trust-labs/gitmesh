---
"@gitmesh/workspace-core": minor
---

Add the third pair of risk rules to the GM table (pivot §8.1 item 3, T1.13). `GM005` (warning) reports the same MCP server defined with different credentials or urls across tools: definitions are collected from every `mcpServers` object (`.mcp.json`, `.cursor/mcp.json`, `.roo/mcp.json`, antigravity `mcp_config.json` and `.gemini/settings.json`, `.ruler/mcp.json`, `.rulesync/mcp.json{,c}`), from `servers` in `.vscode/mcp.json` (read only on `kind: "mcp-config"`), from `mcp` in `opencode.json{,c}`, and from `[mcp_servers.*]` tables in `.codex/config.toml` via the GM001/GM003-precedent tolerant line scan (url, inline and sub-table `env` / `http_headers`); per server name the rule compares the url and every shared env/header key across *different* adapters only (same-adapter layering and the user/managed tiers stay out), a key present on one side only stays silent, and findings name the server, field and files but never a value - not even the urls, which can embed userinfo credentials (hard rule 5) - pinned by a redaction-guarantee test. Third-party-manager MCP sources enter comparison behind a `mcp.json{,c}` path gate (ADR-004: the drift is the finding, management never is). `GM006` (warning) reports a generated-looking file hand-edited - the managed-marker violation heuristic that holds until the T5.x lockfile enables content comparison (ADR-003): a nested opener, a closing `<!-- /gitmesh:managed -->` with no open region, a damaged marker line (deleted `-->`, trailing text, split across lines), or a region never closed before EOF; the marker and fence grammar mirror the §10.4 normalizer, so fenced examples never count, and findings carry no adapter so a file inventoried by several adapters reports once. Four new golden cases under `fixtures/risk/` run the whole table.
